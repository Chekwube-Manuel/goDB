flows:
  - name: Full Workflow
    description: Create tenant, collection, store and retrieve records
    steps:
      - create-tenant
      - create-collection
      - store-record
      - list-records
